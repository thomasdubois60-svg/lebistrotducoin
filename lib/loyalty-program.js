export const defaultLoyaltyProgram={title:'Programme de fidélité permanent',description:'10 formules achetées, la 11e offerte',threshold:10,reward:'Formule offerte',clientText:'10 formules achetées, la 11e offerte',rewardValue:null,enabled:true};
export function normalizeLoyaltyProgram(value){const v=value&&typeof value==='object'?value:{};const n=Number(v.threshold);return {...defaultLoyaltyProgram,...Object.fromEntries(['title','description','reward','clientText'].filter(k=>typeof v[k]==='string').map(k=>[k,v[k]])),threshold:Number.isInteger(n)&&n>=1&&n<=1000?n:10,rewardValue:v.rewardValue!==null&&v.rewardValue!==''&&v.rewardValue!==undefined&&Number.isFinite(Number(v.rewardValue))&&Number(v.rewardValue)>=0?Number(v.rewardValue):null,enabled:v.enabled!==false};}
export function validateLoyaltyProgram(value){if(!value)return;const n=Number(value.threshold);if(!Number.isInteger(n)||n<1||n>1000)throw new Error('Le seuil doit être un entier entre 1 et 1000.');if(value.rewardValue!==null&&value.rewardValue!==''&&value.rewardValue!==undefined&&(!Number.isFinite(Number(value.rewardValue))||Number(value.rewardValue)<0))throw new Error('La valeur de récompense doit être positive ou nulle.');}
export function loyaltyRewardDetails(content){const p=normalizeLoyaltyProgram(content.loyaltyProgram);let value=p.rewardValue;let name=p.reward;if(value===null){const normalize=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').toLowerCase();const formulas=content.daily?.formulas||[];const f=formulas.find(f=>{const n=normalize(f.name);return n.includes('entree')&&n.includes('plat')&&n.includes('dessert')&&!n.includes(' ou ')})||formulas.find(f=>normalize(f.name).includes('complete'))||formulas.at(-1);const match=(f?.price||'').replace(/\s/g,'').match(/(\d+(?:[.,]\d{1,2})?)/);if(!match)throw new Error('Renseignez la valeur de la récompense ou le prix de la formule complète.');value=Number(match[1].replace(',','.'));}return {name,valueTtc:value,formatted:value.toLocaleString('fr-FR',{style:'currency',currency:'EUR'}),rawPrice:String(value)};}
