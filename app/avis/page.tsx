'use client'
import { PageHero } from '@/components/page-hero'
import { useSiteContent } from '@/components/content-provider'
export default function ReviewsPage(){ const {reviews,socials,pageTexts}=useSiteContent(); const active=socials.filter(link=>link.url.trim()); return <><PageHero eyebrow="Votre expérience" title={reviews.title} text={pageTexts.reviewsIntro || reviews.intro}/><section className="section"><div className="container narrow centered"><p className="lead">{reviews.intro}</p><div className="review-actions">{reviews.googleReviewsUrl&&<a className="button" href={reviews.googleReviewsUrl} target="_blank" rel="noreferrer">Voir les avis Google</a>}{reviews.googleReviewWriteUrl&&<a className="button secondary" href={reviews.googleReviewWriteUrl} target="_blank" rel="noreferrer">Laisser un avis</a>}</div>{active.length>0&&<><h2>Suivez-nous</h2><div className="social-grid">{active.map((link,i)=><a className="social-card" key={i} href={link.url} target="_blank" rel="noreferrer"><strong>{link.label}</strong><span>Ouvrir le compte →</span></a>)}</div></>}</div></section></> }
