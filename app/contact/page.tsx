'use client'
import {useLanguage} from '@/components/language-provider'
import { PageHero } from '@/components/page-hero'
import { useSiteContent } from '@/components/content-provider'
export default function ContactPage(){
 const {t,locale}=useLanguage();
 const {general,pageTexts}=useSiteContent(); const query=encodeURIComponent(general.address.replace(/\n/g,' ')); return <><PageHero eyebrow={t("Nous contacter")} title={t("Poussez nos portes")} text={pageTexts.contactIntro}/><section className="section"><div className="container contact-grid"><div className="contact-card"><h2>Le Bistrot Du Coin</h2><p><strong>{t("Adresse")}</strong><br/><a className="preline" target="_blank" rel="noopener noreferrer" href={`https://www.google.com/maps/dir/?api=1&destination=${query}`}>{general.address} ↗</a></p><p><strong>{t("Téléphone")}</strong><br/><a href={`tel:${general.phoneHref}`}>{general.phone}</a></p><p><strong>{t("Email")}</strong><br/><a href={`mailto:${general.email}`}>{general.email}</a></p><p><strong>{t("Horaires")}</strong><br/><span className="preline">{general.hours}</span></p><div className="actions"><a className="button" href={`tel:${general.phoneHref}`}>{t("Appeler")}</a><a className="button secondary" target="_blank" rel="noreferrer" href={`https://www.google.com/maps/dir/?api=1&destination=${query}`}>{t("Itinéraire")}</a></div></div><iframe className="map" title={t("Carte Le Bistrot Du Coin")} loading="lazy" src={`https://www.google.com/maps?q=${query}&output=embed`}/></div></section></> }
