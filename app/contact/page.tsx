'use client'
import { PageHero } from '@/components/page-hero'
import { useSiteContent } from '@/components/content-provider'
export default function ContactPage(){ const {general,pageTexts}=useSiteContent(); const query=encodeURIComponent(general.address.replace(/\n/g,' ')); return <><PageHero eyebrow="Nous contacter" title="Poussez nos portes" text={pageTexts.contactIntro}/><section className="section"><div className="container contact-grid"><div className="contact-card"><h2>Le Bistrot Du Coin</h2><p><strong>Adresse</strong><br/><span className="preline">{general.address}</span></p><p><strong>Téléphone</strong><br/><a href={`tel:${general.phoneHref}`}>{general.phone}</a></p><p><strong>Email</strong><br/><a href={`mailto:${general.email}`}>{general.email}</a></p><p><strong>Horaires</strong><br/><span className="preline">{general.hours}</span></p><div className="actions"><a className="button" href={`tel:${general.phoneHref}`}>Appeler</a><a className="button secondary" target="_blank" rel="noreferrer" href={`https://www.google.com/maps/search/?api=1&query=${query}`}>Itinéraire</a></div></div><iframe className="map" title="Carte Le Bistrot Du Coin" loading="lazy" src={`https://www.google.com/maps?q=${query}&output=embed`}/></div></section></> }
