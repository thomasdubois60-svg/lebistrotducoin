'use client'
import { PageHero } from '@/components/page-hero'
import { useSiteContent } from '@/components/content-provider'
export default function EventsPage(){ const {events,general,pageTexts}=useSiteContent(); return <><PageHero eyebrow="Au Bistrot" title="Événements" text={pageTexts.eventsIntro}/><section className="section"><div className="container events-grid">{events.length===0?<div className="empty-state"><h2>Les prochains événements arrivent bientôt</h2><p>Suivez nos actualités ou contactez-nous pour en savoir plus.</p><a className="button" href={`tel:${general.phoneHref}`}>Nous contacter</a></div>:events.map((event,i)=><article className="event-card" key={i}>{event.image&&<img src={event.image} alt={event.imageAlt||event.title}/>}<div className="event-content"><span className="eyebrow">{event.date}</span><h2>{event.title}</h2><p>{event.description}</p>{event.price&&<strong className="event-price">{event.price}</strong>}</div></article>)}</div></section></> }
